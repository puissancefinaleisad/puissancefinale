/*
 * creation_socket.c
 *
 *  Created on: 16 mai 2019
 *      Author: isabelle
 */
#include "main.h"
#include "cmsis_os.h"
#include "lwip.h"
#include <stdio.h>
#include <sys/socket.h>
//#include<arpa/inet.h>
#include <inet.h>

int creation_socket(int argc , char *argv[])
	  {
	  	int socket_desc;

	  	//Create socket
	  	socket_desc = socket(AF_INET , SOCK_STREAM , 0);
	  	if (socket_desc == -1)
	  	{
	  		printf("Could not create socket");
	  	}
	  	return 0;
	  }
